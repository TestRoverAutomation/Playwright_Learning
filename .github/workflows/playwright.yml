name: Playwright Tests

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

  # ✅ Scheduled main run every day at 04:00 AM UTC
  schedule:
    - cron: '0 4 * * *'

jobs:
  test:
    runs-on: ubuntu-latest
    container:
      image: mcr.microsoft.com/playwright:v1.56.1-noble
    timeout-minutes: 60

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: lts/*
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright browsers
        run: npx playwright install --with-deps  

      - name: Run Playwright tests
        run: npm run test

      - name: Install Java (for Allure)
        run: |
          apt-get update
          apt-get install -y openjdk-11-jre

      - name: Install Allure CLI globally
        run: |
          npm install -g allure-commandline
          echo "$(npm root -g)/.bin" >> $GITHUB_PATH
          allure --version

      - name: Generate Allure report
        if: success() || failure()
        run: allure generate allure-results --clean -o allure-report

      # ✅ Upload Allure report as artifact (safe name)
      - name: Upload Allure report as artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: allure-report-${{ github.head_ref || github.ref_name }}
          path: allure-report/
          retention-days: 30

      # ✅ Deploy Allure report to Netlify (prod for main / preview for branches)
      - name: Deploy Allure report to Netlify
        if: success()
        env:
          NETLIFY_AUTH_TOKEN: ${{ secrets.NETLIFY_AUTH_TOKEN }}
          SITE_ID: ${{ secrets.NETLIFY_SITE_ID }}
        run: |
          # Sanitize branch name (replace / with -)
          BRANCH_NAME=$(echo "${{ github.head_ref || github.ref_name }}" | tr '/' '-')

          if [ "$BRANCH_NAME" == "main" ] || [ "$BRANCH_NAME" == "master" ]; then
            echo "🚀 Deploying production report for $BRANCH_NAME ..."
            npx netlify deploy \
              --site "$SITE_ID" \
              --prod \
              --dir allure-report
          else
            echo "🧪 Deploying preview report for branch: $BRANCH_NAME ..."
            npx netlify deploy \
              --site "$SITE_ID" \
              --alias "$BRANCH_NAME" \
              --dir allure-report
